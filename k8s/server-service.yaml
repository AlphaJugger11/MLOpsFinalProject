apiVersion: v1
kind: Service
metadata:
  name: fl-server-svc
spec:
  selector:
    app: fl-server
  ports:
    - port: 8080
      targetPort: 8080
    - port: 5001
      targetPort: 5001
  type: LoadBalancer
